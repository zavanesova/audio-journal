<h1 class="text-center logo-name"><img src = "/styles/logo.png" class="logo"> Audio Journal</h1>
<div class="grid-container">
    <div class="grid-x grid-margin-x">
        <div class="medium-2 cell"></div>
        <div class="medium-8 cell align-center">
            <div class="table-scroll align-center">
                <table class="scroll hover t-user">
                    <thead>
                        <tr>
                          <th width="870" class="post-title">{{data1.title}}</th>
                        </tr>
                    </thead>
                    <tbody class="user-table">
                    <tr>
                        <td>{{data1.body}}
                            <p class="date">{{moment date=data1.createdAt format="MMMM DD, YYYY"}}</p>
                        </td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="medium-2 cell"></div>
    </div>


    
    {{!-- <div class="grid-margin grid-margin-x">
        <div class="medium 2-cell"></div>
        <div class="medium 8-cell align-center" id="post-div">
            <h3>{{data1.title}}</h3>
            <p>{{data1.body}}</p>
            <p>{{data1.createdAt}}</p>
        </div>
        <div class="medium 2-cell"></div>
    </div>
</div> --}}